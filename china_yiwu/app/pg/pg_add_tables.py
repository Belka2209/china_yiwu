from pg.pg_con import dbConnection
# from pg.pg_query import add_tables_users
db = dbConnection()

def bd_create_tables():
    # Создаем таблицу users
    # db.execute(add_tables_users())
    pass